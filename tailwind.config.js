import colors from 'tailwindcss/colors';

export default {
    theme: {
        extend: {
            fontFamily: {
                sans: ['Lexend']
            },
            borderColor: {
                DEFAULT: colors.gray[400],
            },
        }
    }
}